<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="YourDrink.CategoryListPage">
    <ContentPage.Content>

       
        <ListView x:Name="CategoryList" ItemTapped="CategoryButton_Clicked"  SelectionMode="None">

            <ListView.ItemTemplate>

                <DataTemplate>
                    <ViewCell>
                        <ViewCell.View>
                            <Button x:Name="CategoryButton" ImageSource="Star" BackgroundColor="White" Text="{Binding Name}" Clicked="CategoryButton_Clicked"/>
                            <!--<Grid x:Name="CategoryGrid" ColumnSpacing="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="2*"/>
                                </Grid.ColumnDefinitions>
                              -->
                               <!-- <Image Grid.Column="0"  Source="cocktails" Aspect="AspectFill"/>
                                <Label Grid.Column="1"  x:Name="CategoryButton" TextColor="Black" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" FontSize="Large" BackgroundColor="White" Text="{Binding Name}" />
                                -->
                        
                        </ViewCell.View>

                    </ViewCell>

                </DataTemplate>

            </ListView.ItemTemplate>

        </ListView>
           
    </ContentPage.Content>

</ContentPage>
